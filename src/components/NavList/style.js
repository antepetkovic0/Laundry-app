import styled from 'styled-components';

export const Ul = styled.ul`
  list-style: none;
  margin: 0;
  padding: 0;
  font-size: 1.6rem;
  display: none;


  /* @media ${breakpoint.tablet} {
    display: flex;
  } */
`;